#include<stdio.h>
 int main ()  {

     int h,l,a;
     printf("Enter height and legth of base of triangle");
     scanf("%d",&l);

    a = h*l/2;


     printf("the triangle of height %d m and lenght of base %d m has area %d sq m ",l,h,a);

    return 0;

 }
