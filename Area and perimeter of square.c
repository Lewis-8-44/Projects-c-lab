#include<stdio.h>
 int main ()  {

     int l,a,p;
     printf("Enter length of side of square");
     scanf("%d",&l);

    a = l*l;
    p = 4*l;

     printf("the square of lenght %d m has area %d sq m and perimeter %d m ",l,a,p);

    return 0;

 }
